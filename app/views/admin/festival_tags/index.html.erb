<div class="mx-auto max-w-3xl px-4 py-8 space-y-6">
  <div>
    <h1 class="text-2xl font-bold text-slate-900">タグ管理</h1>
    <p class="mt-1 text-sm text-slate-600">フェスに付与するタグを作成・編集・削除できます。</p>
  </div>

  <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
    <h2 class="text-lg font-semibold text-slate-900">新規タグを作成</h2>
    <div class="mt-3">
      <%= render "form", festival_tag: @festival_tag, submit_label: "作成する" %>
    </div>
  </div>

  <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
    <h2 class="text-lg font-semibold text-slate-900">タグ一覧</h2>
    <% if @festival_tags.any? %>
      <div class="mt-3 divide-y divide-slate-100">
        <% @festival_tags.each do |tag| %>
          <div class="flex items-center justify-between gap-3 py-3">
            <span class="text-sm font-semibold text-slate-800"><%= tag.name %></span>
            <div class="flex items-center gap-2">
              <%= link_to "編集",
                          edit_admin_festival_tag_path(tag),
                          class: "inline-flex items-center rounded border border-slate-300 px-3 py-1 text-xs font-semibold text-slate-700 hover:bg-slate-50" %>
              <%= link_to "削除",
                          admin_festival_tag_path(tag),
                          data: { turbo_method: :delete, turbo_confirm: "削除してよろしいですか？" },
                          class: "inline-flex items-center rounded border border-rose-200 px-3 py-1 text-xs font-semibold text-rose-700 hover:bg-rose-50" %>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <p class="mt-3 text-sm text-slate-600">まだタグがありません。</p>
    <% end %>
  </div>
</div>
