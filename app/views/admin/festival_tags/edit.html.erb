<div class="mx-auto max-w-md px-4 py-8 space-y-6">
  <div>
    <h1 class="text-2xl font-bold text-slate-900">タグを編集</h1>
    <p class="mt-1 text-sm text-slate-600">名称を変更します。</p>
  </div>

  <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= render "form", festival_tag: @festival_tag, submit_label: "更新する" %>
  </div>

  <div>
    <%= link_to "一覧に戻る",
                admin_festival_tags_path,
                class: "text-sm font-semibold text-indigo-600 hover:underline" %>
  </div>
</div>
