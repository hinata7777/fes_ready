<main class="min-h-screen bg-gradient-to-b from-sky-200 via-sky-100 to-white pb-32 text-center">
  <!-- メインビジュアル -->
  <div class="relative w-full min-h-[60vh] md:min-h-[50vh] lg:min-h-[75vh] -mt-16 lg:-mt-28">
    <%= image_tag(
      "fes.png",
      alt: "FES READY ステージ",
      class: "absolute inset-0 h-full w-full object-cover"
    ) %>
    <h1 class="absolute left-1/2 top-[35%] -translate-x-1/2 -translate-y-1/2 text-4xl md:text-6xl font-black tracking-tight text-black drop-shadow-[0_4px_12px_rgba(255,255,255,0.8)]">
      FES READY
    </h1>
  </div>

  <!-- 説明ブロック -->
  <section class="mx-auto w-full max-w-screen-md px-6 py-8">
    <p class="text-sm md:text-base leading-relaxed text-slate-900">
      最高のフェス体験を、これ１つで。
    </p>

    <!-- 使い方（下向き矢印） -->
    <div class="mt-6">
      <p class="text-base font-semibold text-slate-900">使い方</p>
      
      <%= icon 'chevron-down',
          class_name: 'size-10 mx-auto text-slate-700 animate-bounce motion-reduce:animate-none',
          aria: { label: '下へ' } %>
    </div>
  </section>
</main>