<div class="min-h-screen px-4 py-6">
  <div class="mx-auto max-w-5xl space-y-6">
    <header class="space-y-4">
      <h1 class="text-2xl font-bold text-slate-900">マイタイムテーブル一覧</h1>
      <p class="text-sm text-slate-600">作成したマイタイムテーブルを確認できます。</p>
    </header>

    <% if @festival_day_groups.present? %>
      <section class="space-y-6">
        <% @festival_day_groups.each do |festival, days| %>
          <div class="grid gap-3 md:grid-cols-2 md:gap-4">
            <% days.each do |festival_day| %>
              <div class="w-full">
                <%= render Shared::NavStackButtonComponent.new(
                           label: "#{festival.name} - #{format_date_with_weekday(festival_day.date)}",
                           url: festival_my_timetable_path(festival, date: festival_day.date.to_s)
                         ) %>
              </div>
            <% end %>
          </div>
        <% end %>
      </section>
    <% else %>
      <div class="rounded-xl bg-white px-4 py-12 text-center text-sm text-slate-500 shadow">
        まだ保存したマイタイムテーブルがありません。
      </div>
    <% end %>
  </div>
</div>
